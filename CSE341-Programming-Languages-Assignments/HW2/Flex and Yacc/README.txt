in order to run the program, we have makefile
Simply type terminal the followings:
make

After typing make, programs runs but we are not finished yet, if we are reading input from a file, then type this (with $ sign):
$g++ input2.txt

If you don't want to read input from file, then just type (with $ sign):
$g++

then type one line at a time

To exit, type exit

In the input2.txt file, there is an willingly error that declares the variable again. It is intented
